# Requirements Engineering MCP environment configuration
#
# Installed to /etc/reqeng-mcp/env.example.
# Copy to /etc/reqeng-mcp/env and edit for your deployment; the
# systemd unit (/usr/lib/systemd/system/reqeng-mcp.service) sources
# /etc/reqeng-mcp/env via EnvironmentFile=.

# --- Logging ---
# DEBUG | INFO | WARNING | ERROR
FASTMCP_LOG_LEVEL=INFO
# Set to false for plain/structured JSON output instead of Rich formatting.
FASTMCP_ENABLE_RICH_LOGGING=true

# --- Domain configuration ---
# All domain env vars use the REQENG_MCP_ prefix.
# See the README and `reqeng-mcp serve --help` for the full list.
#
# Example:
# REQENG_MCP_EXAMPLE_VAR=value
